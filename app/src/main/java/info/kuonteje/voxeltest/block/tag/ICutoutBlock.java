package info.kuonteje.voxeltest.block.tag;

// Considered solid for meshing, but transparent for face culling
// Fragments discarded if alpha < 0.5, otherwise alpha clamped to 1.0
public interface ICutoutBlock
{
	//
}
